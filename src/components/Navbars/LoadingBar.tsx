import React from 'react'
import LoadingBar from 'react-redux-loading-bar'

const HeaderLoadingBar = () => (
  <header>
    <div className="loading-bar" >
      <LoadingBar />
    </div>
  </header>
)

export default HeaderLoadingBar